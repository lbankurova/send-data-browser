{
  "$schema": "../schemas/profile.schema.json",
  "name": "FDA Default",
  "version": "1.0.0",
  "description": "Standard thresholds aligned with FDA reviewer expectations for IND-enabling nonclinical studies. No overrides — uses all shared/config/ defaults as-is.",
  "regulatory_context": "FDA",
  "overrides": {}
}
