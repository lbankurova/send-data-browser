---
task: findings-dr-merge
status: spec-complete
last_updated: 2026-03-13
---

<current_state>
Spec complete for merging Dose-Response view into Findings view. Branch `merge-findings-dr` created. All open questions resolved. Ready for Phase A implementation.
</current_state>

<completed_work>

This session:
- **Brainstormed and logged merge ideas** → `docs/incoming/view-redesign-ideas.md`
- **Exhaustive gap analysis** (agent-driven, 289 lines) → `docs/incoming/dr-findings-merge-analysis.md`
  - 30 features inventoried, 12 gaps identified, 11 cross-view nav links found
  - Pipeline divergences documented (signal score formula, scheduled-only exclusion)
  - 6 open questions surfaced
- **Full implementation spec** (16 sections, ~500 lines) → `docs/incoming/view-merge-spec.md`
  - All 6 open questions resolved with user:
    - Q1: D-R correlations → OrganContextPanel (organ-level context)
    - Q2: Metrics as mode toggle on FindingsTable (validate pipeline parity first)
    - Q3: Peak detection: argmax |Hedges' g| (continuous), argmin Fisher's p (incidence)
    - Q4: Keep split — ECharts for central panel, SVG for context panel
    - Q5: Migrate bookmarks to rail (TODO if broken)
    - Q6: Tabbed central panel (Evidence + Metrics tabs, Hypotheses killed)
- **Branch created:** `merge-findings-dr`
</completed_work>

<remaining_work>

Implementation in 8 phases (see spec Section 16):

### Phase A: Context Panel Additions (low risk)
1. Add CausalityWorksheet pane to FindingsContextPanel (from D-R Hypotheses tab)
2. Add InsightsList pane (wire `useRuleResults`)
3. Verify ToxFindingForm system suggestion wiring
4. Add incremental header info (pattern badge, assessment status, NOAEL)

### Phase B: Time-Course Unification (medium risk)
5. Extend TimeCoursePane with CL bar chart support
6. Add subject-level trace overlay with dose group filter
7. Add Y-axis mode toggle
8. Add recovery continuity in subject mode (remove group lines)

### Phase C: Central Panel Restructure (high impact)
9. Build DoseResponseChartPanel (D-R + effect size side-by-side, mode toggles)
10. Build FindingsTableHeader (mode toggle)
11. Modify FindingsView for scope-dependent layout + tabbed panel (Evidence + Metrics)
12. Improve FindingsTable columns (data type, headers, tooltips, naming consistency)

### Phase D: Rail Redesign (high impact)
13. Build FindingsNavGrid (flat table/grid with group-by)
14. Wire click behavior (scope filtering, endpoint selection)
15. Migrate endpoint bookmarks

### Phase E: Scatter Merge
16. Merge Pareto/volcano features into FindingsQuadrantScatter at group scope

### Phase F: Pipeline Consolidation
17. Audit and consolidate signal score formulas
18. Verify scheduled-only exclusion on D-R charts
19. Validate day-by-dose table redundancy, then remove

### Phase G: Route Cleanup & Deletion
20. Update all 11 cross-view navigation links
21. Add location.state handling to FindingsView
22. Add redirect route, delete D-R view + components + unused backend

### Phase H: Doc & Test Cleanup
23. Archive D-R view spec, update Findings view spec, MANIFEST, tests
</remaining_work>

<decisions_made>

- Merge D-R into Findings, kill D-R view entirely
- Findings takes precedence for duplicate panes/features
- Flat rail (no nested groups) with group-by toggle
- Central panel is tabbed: Evidence (default) + Metrics
- D-R charts replace scatter at endpoint level; scatter stays at group level
- CausalityWorksheet → context panel pane (above ToxFindingForm)
- D-R correlations → OrganContextPanel (organ-level, not endpoint-level)
- Time-course unified in context panel only, SVG rendering
- Subject mode: recovery continuity (same animal), no group lines, dose group filter required
- Peak detection: argmax |Hedges' g| (continuous), argmin Fisher's p (incidence)
- ECharts for central panel charts, SVG for context panel charts
- Endpoint bookmarks migrate to rail
</decisions_made>

<blockers>
None.
</blockers>

<context>
Key files for implementation:
- Spec: `docs/incoming/view-merge-spec.md` (source of truth for all decisions)
- Ideas log: `docs/incoming/view-redesign-ideas.md`
- Gap analysis: `docs/incoming/dr-findings-merge-analysis.md`
- Branch: `merge-findings-dr`
- D-R view: `frontend/src/components/analysis/DoseResponseView.tsx` (2899 lines — the thing being killed)
- Findings view: `frontend/src/components/analysis/findings/FindingsView.tsx` (432 lines — the target)
- Findings context panel: `frontend/src/components/analysis/panes/FindingsContextPanel.tsx` (1700+ lines)
- D-R context panel: `frontend/src/components/analysis/panes/DoseResponseContextPanel.tsx` (398 lines)
</context>

<next_action>
Start Phase A: Add CausalityWorksheet pane to FindingsContextPanel. Read the CausalityWorksheet implementation in DoseResponseView.tsx (lines 2544-2800), extract it, and render in FindingsContextPanel as a collapsible pane above ToxFindingForm.
</next_action>
</content>
</invoke>