---
task: arch01-discussion-continued
status: in_progress
last_updated: 2026-03-20T16:01:24.278Z
---

<current_state>
ARCH-01 (Endpoint-Level Restructure) discussion continued this session. D5 revised, new D8 added. Q1-Q5 still open but Q4 (central panel tab org) partially resolved by the D5 revision. Three open questions remain fully undiscussed (Q1, Q2, Q3). Q5 untouched.
</current_state>

<completed_work>

Prior sessions (already committed):
- Phase G shipped, perf audit (7 fixes), doc cleanup — all on master
- ARCH-01 D1–D7 decided (2026-03-19)

This session (2026-03-20, discussion only — no code changes):
- **D5 revised:** Central panel is single-view, no tabs. D-R chart (bar/line toggle, ~65%) + effect size/distribution (toggle, ~35%). Effect size is default, distribution on demand.
- **D8 (new):** Time Course demoted from central panel tab to context panel pane. Compact group-mode chart, collapsed by default. Popup (resizable/repositionable) for subject-mode deep dives — user stays on findings view.
- **Day stepper ↔ Time Course sync:** Flagged for design — cursor on time course chart should reflect day stepper selection (and vice versa). Exact interaction TBD.
- **Analytical workflow framework established** — four question clusters mapped to spatial priority:
  1. "Is the effect real and dose-dependent?" → Central panel (table + D-R chart + distribution toggle)
  2. "Is it within normal range?" → Context panel distribution pane (future: historical control)
  3. "Is it part of a bigger pattern?" → Context panel (syndrome flags, correlated endpoints)
  4. "Is it time-dependent / does it recover?" → Context panel time course pane, collapsed, popup for detail
</completed_work>

<remaining_work>

### ARCH-01 open questions:
- **Q1:** Sex collapsing — always show both sexes, collapse when concordant, or combined-default with expand?
- **Q2:** VerdictPane key numbers — per-sex stacked vs lead-sex-prominent
- **Q3:** SexComparisonPane fate — redundant under ARCH-01 or keep as override control center?
- **Q4:** Partially resolved. Distribution placement answered (toggle with effect size). Time course placement answered (context panel + popup). Sub-questions remaining:
  - Q4c: Tab ordering moot (no central panel tabs now). But popup/new-tab behavior for time course needs design.
  - Q4d: Resolved — time course stays as compact context panel pane, no central panel duplicate.
- **Q5:** Incidence endpoints (MI/MA) — should UI behave differently for single-timepoint endpoints?

### Deferred:
- Phase D: Rail Redesign (GAP-96)
- Phase E: Scatter Merge (GAP-97)
- Findings view spec (`docs/views/findings.md`) STALE — needs rewrite after ARCH-01 decisions lock

### Backlog: 74+ open items in TODO.md
</remaining_work>

<decisions_made>

ARCH-01 full decision log:
- D1: Context panel is endpoint-anchored
- D2: Both sexes are first-class citizens
- D3: Active finding = timepoint cursor
- D4: DoseDetailPane removed — pivoted FindingsTable replaces it
- **D5 (revised 2026-03-20):** Central panel = single view, no tabs. Left (~65%): D-R chart with bar/line toggle. Right (~35%): effect size (default) / distribution (toggle). Rationale: effect size is routine, distribution is investigative — toggle correctly frames distribution as "alternate lens." Historical control data (future) goes in context panel distribution pane.
- D6: Day stepper in section header
- D7: FindingsTable endpoint-aware (auto-filter, auto-pivot, column reorder)
- **D8 (new 2026-03-20):** Time Course = context panel pane (compact group-mode, collapsed by default) + popup (resizable/repositionable) for subject-mode investigation. NOT a central panel tab. Rationale: time course is a follow-up question ("is it time-dependent?"), asked after establishing the effect is real — not a primary analytical lens.
- Day stepper ↔ Time Course sync: cursor reflection needed, exact interaction TBD
- Histopath merge deferred
</decisions_made>

<blockers>
None.
</blockers>

<context>
Key files:
- Audit spec: `docs/incoming/findings-view-ux-audit.md` (ARCH-01 at line 1107+)
- FindingsTable: `frontend/src/components/analysis/FindingsTable.tsx`
- FindingsAnalyticsLayer: `frontend/src/contexts/FindingsAnalyticsLayer.tsx`
- DoseResponseChartPanel: `frontend/src/components/analysis/findings/DoseResponseChartPanel.tsx`
- TimeCoursePane: `frontend/src/components/analysis/panes/TimeCoursePane.tsx`

The analytical workflow framework (four question clusters) is the key conceptual output of this session. It should guide all remaining ARCH-01 decisions and the eventual findings view spec rewrite.
</context>

<next_action>
Continue ARCH-01 discussion: Q1 (sex collapsing), Q2 (VerdictPane numbers), Q3 (SexComparisonPane fate), then Q5 (incidence endpoints). Q4 is mostly resolved. After all questions locked, update the ARCH-01 section in the audit spec and rewrite the findings view spec.
</next_action>
