---
phase: exploratory
task: docker-deployment-fix
status: deployed
last_updated: 2026-03-10T21:09:53.316Z
---

<current_state>
Docker deployment to Render (send-data-browser.onrender.com) is fixed and working. App loads but is slow on Render free tier (0.1 CPU / 512MB RAM). User is evaluating upgrade to 512MB/0.5CPU or 2GB/1CPU tier.
</current_state>

<completed_work>

- Diagnosed FileNotFoundError: `/shared/syndrome-definitions.json` — 5 backend modules used `Path(__file__).parents[3]` which resolves to `/` in Docker (backend/ directory level stripped by `COPY backend/ ./`)
- Fixed: Added `SHARED_DIR` to `config.py` with env var + fallback, updated all 5 modules (`syndrome_definitions.py`, `adversity_dictionary.py`, `organ_thresholds.py`, `hcd.py`, `progression_chains.py`) to use centralized config
- Fixed: Added `OPENBLAS_NUM_THREADS=1` to Dockerfile (prevents numpy import hang in constrained containers)
- Fixed: Added `COPY shared/ /shared/` to Dockerfile stage 2
- Added startup verification: shared files checked at boot, analysis pipeline eagerly imported — failures now show `[FATAL]` in deploy logs instead of silent 502
- Added build-time verification: Dockerfile `RUN ls` checks shared files exist before deploy
- Added `ENV SHARED_DIR=/shared` to Dockerfile for explicitness
- Syndrome correlation validation feature was already committed (7897712) from previous session — specs archived
- Commits: 773fabd (COPY shared/), a7b9af6 (OPENBLAS), d14d849 (centralized SHARED_DIR + startup verification)
</completed_work>

<remaining_work>

- User needs to pick Render tier (512MB/0.5CPU recommended as first try, 2GB/1CPU if still slow)
- Uncommitted: `docs/incoming/volcano-effect-axis-fix.md` (trimmed spec), deleted spec files in `docs/incoming/` (already archived)
- Uncommitted: `FindingsRail.tsx` working tree changes (syndrome covariation display in rail cards — GAP-66/67/68)
- Uncommitted: `1e3d4a5` (dose detail severity label fix) not pushed to remote
- STATE.md is stale — still references "spec_complete" from before implementation
</remaining_work>

<decisions_made>

- Centralized `SHARED_DIR` in config.py instead of per-file `parents[3]` — eliminates class of Docker path bugs
- Eager pipeline import at startup — fail fast with clear diagnostics in deploy logs
- Recommended 512MB/0.5CPU as first upgrade (0.1 CPU is the bottleneck, not RAM)
</decisions_made>

<blockers>
None — deployment works, performance is a Render tier issue.
</blockers>

<context>
The deployment debugging consumed the session. No feature work done. The root cause was that `COPY backend/ ./` in the Dockerfile strips one directory level, making `Path(__file__).parents[3]` resolve to `/` instead of the project root. Five shared data files (JSON/YAML) were invisible to the backend at runtime, crashing all analysis endpoints. The fix centralizes path resolution through config.py with an environment variable override.
</context>

<next_action>
Push unpushed commit 1e3d4a5 (dose detail severity fix). Then pick up feature work — either volcano-effect-axis-fix implementation or a TODO.md item. Check Render performance after tier upgrade.
</next_action>
