---
task: findings-invariant-audit
status: spec_complete
last_updated: 2026-03-12T21:19:58.356Z
---

<current_state>
Spec written and approved. Implementation not started. This session fixed 2 critical bugs
(brain self-normalization + organ weight method naming lie) and wrote the spec for a
systematic invariant audit to prevent this class of bug permanently.
</current_state>

<completed_work>

This session:
- **Brain self-normalization fix + organ weight rename** (committed as 5837405):
  - 3-layer brain guard: generation (findings_om.py), pipeline (parameterized_pipeline.py), frontend fallback
  - Stored `active_metric` at generation time (pre-ANCOVA metric for primary stats)
  - Generation-time assertion: `alternatives = computable_metrics - {active_metric}`
  - Renamed default from "absolute" (misleading no-op) to "recommended" (honest no-op)
  - "absolute" now a true override — added to `_OM_METHOD_MAP`
  - Removed auto-set effect in StudySummaryView.tsx
  - Dropdown relabeled: "Per-organ recommended" / "Force absolute" / etc.
  - 3 new backend tests, updated frontend test assertions, all 1430+18 tests pass
- **Recovery tooltip** for low-confidence asterisk (in same commit)
- **Invariant audit spec** written: `docs/incoming/findings-invariant-audit.md`
  - 25 checks in 7 categories (A: data integrity, B: normalization, C: stats, D: classification, E: cross-domain, F: recovery, G: enrichment)
  - Memory entry at `memory/project_invariant_audit.md`

Prior sessions: SLA sweep (19/19 resolved), control-only leakage fix, view audits
</completed_work>

<remaining_work>

### Task 1: Implement invariant audit (Phase 1)
- Create `backend/tests/test_findings_invariants.py`
- Load `generated/PointCross/unified_findings.json`
- Implement 25 checks from spec categories A-G
- Run, triage violations (bugs vs known limitations)
- Fix any new bugs found

### Task 2: Permanent test (Phase 2)
- Parameterize by study
- Document known-limitation exclusions

### Uncommitted
- `docs/incoming/findings-invariant-audit.md` (commit with implementation)
- `docs/incoming/dose-response-view-audit.md` (view audit updates, prior session)
- `docs/incoming/histopathology-view-audit.md` (view audit updates, prior session)
- `docs/knowledge/scientific-logic-review.md` (review updates, prior session)
</remaining_work>

<decisions_made>

- Invariant-based output validation > more unit tests — the bug pattern is cross-module contracts
- "recommended" not "default" for the new no-op value — "default" is ambiguous
- No sessionStorage migration for rename — pre-release internal tool, acceptable
- Brain guard kept as defense-in-depth even after rename fixes root cause
- Generation-time assertions preferred over runtime checks — fail loudly at generation
</decisions_made>

<blockers>
None.
</blockers>

<context>
User is frustrated that 3 critical silent-corruption bugs slipped past 1430+ tests in one week.
All shared the same shape: modules correct in isolation, assembled output wrong. The invariant
audit is high priority — it's the mechanism that catches cross-module contract violations.

Key context: the D2 confidence bug (dda9629, committed by user today) shows the pattern clearly —
classification.py produces `monotonic_increase`, confidence.py expected `monotonic_up`. Every
confidence grade was silently degraded. No unit test caught it because each module was tested
with its own string literals.
</context>

<next_action>
Read `docs/incoming/findings-invariant-audit.md` for the full spec, then implement
`backend/tests/test_findings_invariants.py` starting with category A (data integrity)
checks — simplest and most likely to surface new bugs. Run after each category to see
violations incrementally. Key files to read: the spec lists 8 source files.
</next_action>
