---
task: sla-fix-continuation
status: in_progress
last_updated: 2026-03-11T22:55:49.963Z
---

<current_state>
SLA accessor migration sweep (SLA-01/06/12/13) complete and committed as e3b5fc0.
19/19 SLA findings now addressed: 17 resolved, 1 medium (organ-level aggregate), 2 low (SLA-02/07).

Remaining SLA work:
- SLA-09: Incidence quality checks (endpoint-confidence.ts) — P2 correctness
- SLA-18: Recovery vocabulary harmonization — P2 correctness
- SLA-01 organ-level aggregate: deriveOrganSummaries() mixes Cohen's d and avg_severity for mixed-domain organs — P2 medium (next sprint per user)
- SLA-02 frontend signal score compensation — P3
- SLA-07 Math.round grade display — P3
</current_state>

<completed_work>

This session:
- SLA-01: NoaelDecisionView organ header — domain-aware "Max |d|" / "Max avg sev" label via CONTINUOUS_DOMAINS check; endpoint tooltips with effectSizeLabel(domain)
- SLA-01: NoaelDeterminationView — endpoint tooltips with effectSizeLabel(domain) (organ header was already fixed in prior session)
- SLA-06: DoseResponseView computeSignalScore() — zeros effect component for non-continuous data types (MI avg_severity, incidence null not comparable to Cohen's d)
- SLA-06: DoseResponseView computeStrength() — p-value-driven levels for categorical, capped at CATEGORICAL_MAX_STRENGTH_LEVEL=3 (named constant with doc comment per user feedback)
- SLA-06: DoseResponseView volcano scatter — replaced local INCIDENCE set with INCIDENCE_DOMAINS import from domain-types.ts
- SLA-06: DoseResponseView generateConclusion() — uses effectSizeLabel(domain) instead of hardcoded "max effect size"
- SLA-12: OrganRailMode severity filter — replaced local HISTO_DOMAINS with CONTINUOUS_DOMAINS.has() positive check (unknown domains default to requiring severity)
- SLA-13: DoseResponseView + FindingsTable effect cells — domain-aware tooltips via effectSizeLabel(domain)
- SLA-13: DoseResponseEndpointPicker — guards |d|= display with CONTINUOUS_DOMAINS check, added tooltip
- Post-implementation review completed with user feedback incorporated:
  - Gap 1 bumped from Low to Medium per user (organ-level aggregate misleads pathologists skimming at organ row level)
  - CATEGORICAL_MAX_STRENGTH_LEVEL extracted as named constant per user feedback
  - TODO.md updated with all resolutions and remaining items

Prior sessions:
- SLA-15: CL recovery MIN_RECOVERY_N guard (ac748e5)
- SLA-16: Corroboration direction coherence (ac748e5)
- SLA-17: INCIDENCE_DOMAINS registry (b2b7c58)
- SLA-19: Typed accessor functions in domain-types.ts (b2b7c58)
- Scientific logic audit: 8 seams, 19 deduplicated findings
- Histopathology view audit (24 findings), dose-response view audit (19 findings)
</completed_work>

<remaining_work>

- SLA-09: Incidence quality checks — checkNonMonotonic() and checkTrendTestValidity() in endpoint-confidence.ts silently skip incidence data. Need incidence-appropriate checks.
- SLA-18: Recovery vocabulary harmonization — 7 canonical verdicts across continuous/histopath/CL
- SLA-01 organ aggregate (Medium, next sprint): deriveOrganSummaries() in NoaelDecisionView.tsx:94 and NoaelDeterminationView.tsx computes maxEffectSize as Math.abs(row.effect_size) across ALL domains. Fix: track separate per-metric-type max.
- SLA-02 frontend (P3): computeEndpointSignal() effectWeight compensation for incidence
- SLA-07 (P3): Math.round(maxGrade) for INHAND display label
- ~15 internal-logic callsites still read maxEffectSize directly (deferred — sorting/scoring internals, not user-facing)
</remaining_work>

<decisions_made>

- Tooltip-first approach for mixed-domain tables: cell-level tooltips rather than dynamic column headers
- Positive CONTINUOUS check in OrganRailMode rather than negating INCIDENCE — unknown future domains default to requiring severity (safer)
- P-value-driven categorical strength capped at level 3 (CATEGORICAL_MAX_STRENGTH_LEVEL) — MI avg_severity on 1-5 scale would inflate through Cohen's d thresholds
- User bumped organ-level aggregate gap from Low to Medium — pathologists skim at organ row level, misleading maxEffectSize is the same SLA-01 problem one level up
- User confirmed ~15 internal callsite deferral is correct — sorting/scoring internals aren't misrepresenting data to users
</decisions_made>

<blockers>
None.
</blockers>

<context>
The SLA accessor migration sweep is the mechanical completion of Phase 0. The key display callsites are now migrated. Remaining work is either correctness fixes (SLA-09/18) requiring new logic, or the organ-level aggregate which needs a structural change to deriveOrganSummaries(). The user's prioritization: SLA-09/18 are P2 correctness, organ aggregate is medium next-sprint, SLA-02/07 are P3.
</context>

<next_action>
User should choose between:
1. SLA-09: Add incidence-appropriate quality checks to endpoint-confidence.ts (correctness)
2. SLA-18: Recovery vocabulary harmonization across continuous/histopath/CL (correctness)
3. SLA-01 organ aggregate: Separate per-metric-type max in deriveOrganSummaries() (medium, next sprint)
4. Pick up other work from TODO.md backlog
</next_action>
