---
task: findings-invariant-audit
status: completed
last_updated: 2026-03-12T22:30:00Z
---

<current_state>
Findings invariant audit is COMPLETE (Phase 1 + Phase 2). 32 invariant checks across 7 categories, parameterized by study (auto-discovers from generated/). Spec archived.
</current_state>

<completed_work>

- **Phase 1** (committed aaf2f1b): 32 invariant checks in 7 categories (A–G)
- **Phase 2** (this session): Study parameterization via `_discover_studies()` + `study_name` fixture. All tests auto-run per study. D5/G1/G2/G3 updated from hardcoded "PointCross" to `study_name` fixture.
- **Spec archived**: `docs/incoming/findings-invariant-audit.md` → `docs/incoming/archive/`
- **3 pre-existing test failures** fixed in `test_per_sex_integrity.py` (SLA-16 partially_corroborated)
</completed_work>

<remaining_work>

- **TODO.md backlog**: 76+ open items
- **3 crashing test files** (pre-existing, unrelated): `test_early_death_exclusion.py`, `test_mortality.py`, `test_pk_integration.py` call `sys.exit()` at module level
</remaining_work>

<decisions_made>

- Auto-discover studies via `GENERATED_DIR.iterdir()` filtered by `unified_findings.json` existence
- Module-scoped `study_name` fixture parameterizes all 32 tests per study
- Graceful fallback: if no studies exist, all tests skip with reason "No generated studies"
- Known data pattern exclusions kept inline in test docstrings (not extracted to separate file — docstrings are more readable and the patterns are study-agnostic)
</decisions_made>

<blockers>
None.
</blockers>

<context>
Invariant audit is fully done. Next work: pick from TODO.md backlog.
</context>

<next_action>
Pick from TODO.md backlog.
</next_action>
