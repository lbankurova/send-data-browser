---
phase: exploratory
task: performance-optimization
status: completed
last_updated: 2026-03-11T20:10:51.791Z
---

<current_state>
Two commits pushed this session. Performance optimization pass complete. Several unstaged changes from prior sessions remain in the working tree.
</current_state>

<completed_work>

- GAP-58: Removed hard min/max limits on resizable panels (nav tree, rail, context panel). Consolidated Layout.tsx's duplicate useResize into shared hook with options-object API. 9 tests added. Commit 44de6f4.
- Performance pass (6 items, commit 30c61f5):
  1. Removed unused scikit-posthocs from requirements.txt (~8s startup savings)
  2. Short-circuited _apply_overrides when no pattern overrides exist (skip 418 shallow copies)
  3. Compact JSON from generator — removed indent=2 (42% smaller unified_findings: 2.69MB -> 1.54MB)
  4. Installed orjson + set ORJSONResponse as FastAPI default (5-10x faster serialization)
  5. Stripped raw_subject_values from unified-findings API responses (12% payload reduction)
  6. Added prefetch of unified-findings + lesion-severity-summary during StudySummaryView
- README.md updated: removed scikit-posthocs, added orjson to tech stack
- Backend-architect agent ran full architecture review — findings saved in agent context (af4e23338d4d4cc3e)
</completed_work>

<remaining_work>

## Unstaged changes from prior sessions (not part of this session's work)
- Multiple context panel rewrites (FindingsContextPanel, HistopathologyContextPanel, NoaelContextPanel, OrganContextPanel, SyndromeContextPanel, ValidationContextPanel, StudyDetailsContextPanel, DoseResponseContextPanel)
- SubjectProfilePanel.tsx changes
- MortalityDataSettings.tsx changes
- docs/TODO.md changes
- docs/knowledge/scientific-logic-review.md date bump
- New untracked files: NoaelDecisionView.tsx, ContextPanelHeader.tsx, usePaneHistory.ts

## Deferred performance items (intentionally excluded)
- _confidence stripping: needs UI verification pass first (26% of payload, but must grep all _confidence keys and verify each view)
- Duplicate LRU cache consolidation (analyses.py + analysis_views.py): code quality issue, do during next refactoring pass
- Deferred pipeline import: only matters if cold starts become a problem again

## Stale checkpoint
- The .continue-here.md from the Render performance diagnosis (phases 2-5 plan) is now obsolete — phases 2/3/4 were implemented in prior commits (fd40ea7, 98ee5dc). Phase 1 (SPA split) and Phase 5 (bake overrides) remain unimplemented but are low priority.
</remaining_work>

<decisions_made>

- useResizePanel API changed from positional args to options object — breaking internal change but only 3 callers, all updated
- orjson set as app-wide default_response_class (not per-router) — all endpoints benefit
- raw_subject_values stripped at API layer only, not removed from generated JSON — field-contract-sync test reads from disk and still passes
- Prefetch only fires when settings are default (params === "") — avoids triggering pipeline computation eagerly
- scikit-posthocs confirmed unused via exhaustive grep before removal
- Portability docs (docs/portability/) left mentioning scikit-posthocs — they're frozen historical references per CLAUDE.md rule 7
</decisions_made>

<blockers>
None.
</blockers>

<context>
Session started with /gsd:resume-work. User asked about traces of a prior UX audit on histopathology (lost to abrupt session close — no artifacts found). Then implemented GAP-58 (panel resize limits), then invoked backend-architect plugin agent for a full architecture review, then implemented 6 performance optimizations sequentially with verification at each step. User enforces strict sequential implementation with verification, post-implementation review, and explicit commit approval.
</context>

<next_action>
Review and triage the unstaged context panel rewrites from prior sessions. These appear to be significant UI work that was never committed. Start by diffing each file to understand the scope, then decide whether to commit as-is or test first.
</next_action>
