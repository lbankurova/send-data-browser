---
task: sla-fix-continuation
status: in_progress
last_updated: 2026-03-11
---

<current_state>
SLA-15 and SLA-16 implemented and tested. 17/19 SLA findings now resolved.

Remaining SLA work:
- SLA-09: Incidence quality checks (endpoint-confidence.ts) — P2
- SLA-18: Recovery vocabulary harmonization — P2
- Accessor migration sweep: ~20+ maxEffectSize callsites + SLA-01/06/12/13 label fixes — P2 mechanical
- SLA-02 frontend signal score compensation — P3
- SLA-07 Math.round grade display — P3 (ECETOC magnitude label only, no export impact)
</current_state>

<completed_work>

- Scientific logic audit: 8 seams audited via parallel agents, 19 deduplicated findings
- Root cause: `max_effect_size` polymorphic field (Cohen's d / avg_severity / null)
- User provided initial fix spec; reviewed against actual codebase; found 8 disagreements
- Updated fix spec with corrected implementations:
  - SLA-05: Branch B-factors on data_type (not "any incidence > 0 = adverse")
  - SLA-06: Use computeWithinTypeRank() or filter incidence out (not facets)
  - SLA-02: Redistribute signal score weights by data_type (not raw incidence rate normalization)
  - SLA-04: Fix only deriveStatisticalConfidence() (not whole ECI)
  - SLA-11: Deduplicate numerator, keep signal-weighted average (not domain ratio)
  - SLA-13: Show OR for all incidence domains (not just low-incidence)
  - SLA-14: Skip penalty for non-continuous entirely (not "MI grade >= 3 only")
  - SLA-18: 7-term harmonized vocabulary (not 4-term collapse)
- Prior sessions: histopathology view audit (24 findings), dose-response view audit (19 findings)
</completed_work>

<remaining_work>

- Phase 0: Domain-type registry + typed accessors (Python + TypeScript), ~30 callsite migration
- Phase 1: S1 fixes (ECETOC B-factors, labels, volcano chart, syndrome magnitude)
- Phase 2: Signal score weights, confidence, evidence score dedup, convergence groups
- Phase 3: Sorting, severity filter, CL recovery, odds ratio display
- Phase 4: Quality checks, NOAEL penalty, corroboration, vocabulary
- Triage all three audits → TODO.md as BUG-/GAP- items
</remaining_work>

<decisions_made>

- SLA fix spec reviewed and corrected — see sla-fix-spec.md for all decisions with rationale
- 5 DR view must-fix items share root cause with SLA findings — fixing SLA cascades to DR view
- data_type field already exists in JSON contract — no schema migration needed
- Frontend/backend signal score formulas are completely different — both need parallel fixes
</decisions_made>

<blockers>
None.
</blockers>

<context>
Phase 0 is the foundation — create DOMAIN_EFFECT_TYPE registry in send_knowledge.py and TypeScript equivalents, then grep ~30 callsites for max_effect_size/maxEffectSize and replace with typed accessors. Every subsequent phase fix becomes a simple substitution after that. The key insight from the review: the original fix spec proposed several oversimplified solutions that would have broken the existing sophisticated classification system (adaptive trees, intrinsic adversity dictionary, ECETOC tiers). The corrected spec preserves the existing sophistication while fixing the data-type blindness.
</context>

<next_action>
Start Phase 0: Create DOMAIN_EFFECT_TYPE in send_knowledge.py, typed accessors (Python + TypeScript), grep all max_effect_size callsites, replace with accessors.
</next_action>
