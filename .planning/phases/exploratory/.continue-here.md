---
task: findings-invariant-audit
status: in_progress
last_updated: 2026-03-12T22:14:53Z
---

<current_state>
Phase 1 of findings invariant audit is fully implemented and committed (aaf2f1b). 32 invariant checks across 7 categories (A–G) in `backend/tests/test_findings_invariants.py`. All 418 PointCross findings pass all checks. Three pre-existing test failures in `test_per_sex_integrity.py` also fixed (SLA-16 introduced `partially_corroborated` without updating tests). Phase 2 (parameterize by study, formalize known-limitation exclusions) and spec archival remain.
</current_state>

<completed_work>

This session:
- **Findings invariant audit Phase 1** (committed aaf2f1b):
  - 32 invariant checks in 7 categories (A: data integrity, B: normalization contract, C: statistical consistency, D: classification pipeline, E: cross-domain consistency, F: recovery data, G: enrichment pipeline)
  - Category A: 6 checks (A1–A6) — continuous means vary, SDs non-zero, sample sizes positive, control group exists, pairwise only treated, no control-only findings
  - Category B: 4 checks (B1–B4) — active_metric set, alternatives complement, brain excludes ratio_to_brain, alternative stats differ
  - Category C: 6 checks (C1–C6) — direction matches data, p-values valid, effect size sign, min_p_adj matches pairwise, max_effect_size matches pairwise, trend completeness
  - Category D: 5 checks (D1–D5) — confidence dimensions non-trivial, canonical patterns, severity consistency, signal score non-negative (in study_signal_summary.json), signal coverage
  - Category E: 5 checks (E1–E5) — OM specimens, LB test_codes, incidence range, sex valid, no duplicates
  - Category F: 3 checks (F1–F3) — recovery fields structural (verdicts computed at runtime, not stored)
  - Category G: 3 checks (G1–G3) — signal summary completeness, target organ summary completeness, severity consistency across files
  - Known data patterns triaged and documented: LB day-30 baselines (37 control-only), single-survivor day-90 findings (43 no-control), GGT empty group placeholders (4 n=0)
- **Fixed 3 pre-existing test failures** in `test_per_sex_integrity.py`:
  - `test_heart_weight_m_still_corroborated`: asserts `partially_corroborated` (SLA-16 direction coherence gate)
  - `test_all_findings_have_corroboration_status`: added `partially_corroborated` to valid set
  - `test_corroborated_count`: counts full + partial (61 + 15 = 76)

Prior sessions:
- All SLA work complete
- Hedges' g labeling fix (6478524)
- BUG-19: Control-only pipeline leak fixed (35c92aa)
</completed_work>

<remaining_work>

- **Phase 2 of invariant audit**: parameterize tests by study (not just PointCross), formalize known-limitation exclusions with structured documentation
- **Spec archival**: archive `docs/incoming/findings-invariant-audit.md` per doc lifecycle rule
- **TODO.md backlog**: 76+ open items to pick from
- **3 crashing test files** (pre-existing, unrelated): `test_early_death_exclusion.py`, `test_mortality.py`, `test_pk_integration.py` call `sys.exit()` at module level, crash pytest collection
</remaining_work>

<decisions_made>

- Adapted spec checks F1–F3: recovery verdicts computed live by API, not stored on findings → check structural fields (`has_recovery_subjects`, `separate_*`) instead
- Adapted spec checks D4/G1–G3: `signal_score` lives in `study_signal_summary.json`, not on unified_findings → check that file instead
- Canonical dose-response patterns: `monotonic_increase`, `monotonic_decrease`, `threshold_increase`, `threshold_decrease`, `non_monotonic`, `flat`, `insufficient_data` (not `monotonic_up`/`threshold_up` — those were the D2 bug)
- `max_effect_size` is signed (the cohens_d with largest absolute value, preserving sign) — C5 uses `abs()` comparison
- Confidence baseline is MODERATE (sum=0), so all-neutral dimension scores are legitimate — D1 adapted to not flag these
- A4 (control group exists) excludes day-90 single-survivor findings and LB day-30 baselines as known patterns
</decisions_made>

<blockers>
None.
</blockers>

<context>
The invariant audit was motivated by 3 silent-corruption bugs found in one week (brain self-normalization, D2 pattern mismatch, control-only leakage). All 3 prior fixes are holding — no new bugs found. The 32 checks now serve as a regression net against future cross-module contract violations. Phase 2 is lower priority since PointCross is the only study currently.
</context>

<next_action>
Pick from TODO.md backlog or implement Phase 2 of invariant audit (parameterize by study).
</next_action>
