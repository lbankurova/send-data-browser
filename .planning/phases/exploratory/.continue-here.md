---
phase: exploratory
task: syndrome-correlation-validation-spec
status: spec_complete
last_updated: 2026-03-10T15:00:08.638Z
---

<current_state>
Spec for syndrome correlation validation pane is written and ready for implementation. No code changes made this session — purely review + planning.
</current_state>

<completed_work>

- Post-implementation review of `organ-correlation-matrix.md` — all 16 acceptance criteria pass
- Documented 2 intentional deviations as acceptable (no loading skeleton, "no individual data" message hidden)
- Archived `organ-correlation-matrix.md` to `docs/incoming/archive/`
- Read `correlation-context-strategy.md` — the pre-spec analysis document
- Resolved all open questions from strategy doc (lazy vs precomputed, mixed data types, thresholds)
- Wrote full implementation spec: `docs/incoming/syndrome-correlation-validation.md`
  - 7 resolved decisions with rationale
  - 4 implementation phases
  - Acceptance criteria, syndrome member table, architecture diagram
  - Key decision: lazy computation via POST endpoint, thresholds aligned with organ matrix (0.7/0.4)
</completed_work>

<remaining_work>

- Implement `docs/incoming/syndrome-correlation-validation.md` (4 phases):
  - Phase 1: Backend — `compute_syndrome_correlations()` in correlations.py, `build_syndrome_correlation_summary()` in context_panes.py, schemas, POST endpoint
  - Phase 2: Frontend hook — `useSyndromeCorrelations.ts`, `fetchSyndromeCorrelations()`, TypeScript types
  - Phase 3: Frontend pane — `SyndromeCorrelationPane.tsx`, integrate into SyndromeContextPanel
  - Phase 4: Polish + update `docs/systems/correlation-matrix.md` with syndrome section
- Log follow-on items to TODO.md (validation label in rail cards, confidence adjustment, precomputed fallback)
</remaining_work>

<decisions_made>

- Lazy (on-request) computation over precomputed — frontend owns syndrome detection, small computation, most panels never opened
- Thresholds aligned with organ matrix (0.7/0.4) — prevents contradictory labels for overlapping pairs in XS01/XS03/XS04
- POST endpoint (not GET) — variable-length endpoint_labels list
- Adapter in the hook — maps SyndromeCorrelationResult to OrganCorrelationMatrix format so CorrelationMatrixPane is reused unmodified
- Cross-organ navigation via selectFinding() — global selection works without special handling
- Pane collapsed by default — secondary validation signal
- syndrome_id in POST body is cache key only, not backend-resolved (comment in schema)
</decisions_made>

<blockers>
None.
</blockers>

<context>
The correlation-context-strategy.md is the parent analysis doc. It identifies two expansion points: organ-system matrix (done, shipped, archived) and syndrome validation (this spec). The strategy doc itself remains in docs/incoming/ as a live reference — it's pre-spec analysis, not an implementation spec.

The SyndromeContextPanel is 2,563 lines with 9 existing panes. The new pane slots in after Evidence, before Dose-response. It reuses CorrelationMatrixPane via an adapter pattern.
</context>

<next_action>
Start with: Implement Phase 1 — backend computation. Begin with `compute_syndrome_correlations()` in `backend/services/analysis/correlations.py`, then schemas, then the POST endpoint.
</next_action>
