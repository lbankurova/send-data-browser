---
task: frontend-n-value-integrity
status: in_progress
last_updated: 2026-03-12T23:18:11Z
---

<current_state>
Fixed BW N-inflation bug in DoseResponseContextPanel (N=401 instead of ~30). Bug fix coded and tested but NOT yet committed. Next: write comprehensive frontend N-value integrity test covering ALL UI surfaces, then commit both together.
</current_state>

<completed_work>

This session:
- **Invariant audit Phase 2** (committed a3cecf6): parameterized by study, spec archived
- **BW N-inflation bug found and fixed** (NOT YET COMMITTED):
  - Root cause: `DoseResponseRow` dropped `day` field, so `doseLevelBreakdown` in context panel summed N across all 15 BW measurement days (15 days × 2 sexes × ~15 subjects = ~401)
  - Fix: added `day: number | null` to `DoseResponseRow` type (`analysis-views.ts:188`)
  - Fix: `flattenFindingsToDRRows` now populates `day` from finding (`derive-summaries.ts:289`)
  - Fix: `doseLevelBreakdown` memo filters to peak-effect day when multiple days exist (`DoseResponseContextPanel.tsx:116-133`)
  - Build passes, 1430 tests pass
- **Comprehensive audit of all N-emitting UI surfaces** (research complete, see below)
- **Study design bounds established** for PointCross:
  - Main study: 10M+10F per dose group × 4 groups = 80
  - Recovery: 5M+5F per dose group × 4 groups = 40
  - TK: 5M+5F per dose group × 3 groups = 30 (no control TK)
  - Pooled (main+recovery): 15M+15F per dose group = 30/group, 120 total
</completed_work>

<remaining_work>

- **Commit the BW N-inflation fix** (3 files changed, build+tests pass)
- **Write comprehensive frontend N-value integrity test** (`frontend/tests/n-value-integrity.test.ts`):
  - Core invariant: every N the UI displays must match the source finding's group_stats N exactly
  - No aggregation may sum across measurement days
  - Cover ALL surfaces: `flattenFindingsToDRRows`, `doseLevelBreakdown`, `deriveEndpointSummaries`, histopath incidence (HistopathologyContextPanel), recovery stats (IncidenceDumbbellChart), signal summary, DoseDetailPane, FindingsTable, StudyTimeline
  - Verify: affected ≤ N everywhere, N ≤ study design bounds per context
  - Use dose_groups as authoritative N source (pooled_n_male=15, pooled_n_female=15, pooled_n_total=30 per dose level)
  - dose_response_metrics.json has 1432 pre-computed rows with N values — can use as test input
</remaining_work>

<decisions_made>

- Peak-effect-day filter is a reasonable heuristic for doseLevelBreakdown, but user wants the TEST to enforce the deeper invariant: every emitted N must equal the known correct value, not just be within bounds
- User's key insight: "if anything emits N that is not equal to the n that should appear for each group/study total/etc (as applicable) - which we know exactly what that number should be based on user settings - then it's an error"
- The backend invariant audit (32 checks) correctly validates generated data, but this bug was a frontend derivation bug — correct data consumed incorrectly. Need a parallel frontend invariant layer.
</decisions_made>

<blockers>
None.
</blockers>

<context>
User pointed out that despite building 32 backend invariant checks, the first thing they looked at in the UI had a bug. The invariant audit validates the data layer but not the presentation layer. The BW bug is `flattenFindingsToDRRows` dropping the `day` field, causing `doseLevelBreakdown` to sum N across 15 measurement days. This is the same bug pattern as D2 (producer/consumer contract mismatch) — just at the frontend layer.

All N-emitting UI surfaces were mapped by explore agent (see completed_work). Key surfaces: DoseResponseContextPanel.doseLevelBreakdown, FindingsTable, DoseDetailPane, HistopathologyContextPanel, HistopathologyView, IncidenceDumbbellChart, StudyTimeline, signal summary. Study design: PointCross has pooled 15/sex/group (main+recovery), 30 combined/group.
</context>

<next_action>
1. Commit the BW N-inflation fix (3 files: analysis-views.ts, derive-summaries.ts, DoseResponseContextPanel.tsx)
2. Write `frontend/tests/n-value-integrity.test.ts` — comprehensive N-value correctness test across all UI surfaces
3. Run tests, commit together or separately
</next_action>
