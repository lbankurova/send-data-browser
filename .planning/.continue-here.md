---
phase: ad-hoc perf + chart fixes
task: 3
total_tasks: 3
status: completed (2/3), spec'd (1/3)
last_updated: 2026-03-07T14:02:16.524Z
---

<current_state>
Two fixes shipped in commit 508fc69. Third item (strip plot for single-timepoint endpoints) is fully spec'd in `.planning/handoff-strip-plot.md` but not implemented — needs a new session due to context limits.
</current_state>

<completed_work>

- Task 1: Lazy validation startup — Done (commit 508fc69)
  - `backend/routers/validation.py`: init_validation() no longer runs full validation at startup. Validation deferred to first GET request per study. Startup: >60s → ~1s.
- Task 2: Recovery dumbbell chart resize — Done (commit 508fc69)
  - Created `frontend/src/hooks/useContainerWidth.ts` (ResizeObserver hook)
  - `RecoveryDumbbellChart.tsx` + `IncidenceDumbbellChart.tsx`: SVG viewBox width now tracks actual container width instead of hardcoded 200. Charts maintain fixed row height at any panel width.
- Task 3: Strip plot for single-timepoint endpoints — Spec'd, not implemented
  - Full handoff at `.planning/handoff-strip-plot.md`
</completed_work>

<remaining_work>

- Strip plot implementation (see `.planning/handoff-strip-plot.md`):
  1. Backend: new endpoint for per-subject values (`/api/studies/{study_id}/domain-values/{domain}/{test_code}`)
  2. Frontend: `useSubjectValues` hook
  3. Frontend: `StripPlotChart` component (individual animal dots by dose group, F/M panels)
  4. Frontend: integrate into `TimeCoursePane` — render strip plot when totalTimepoints < threshold
</remaining_work>

<decisions_made>

- Lazy validation (Option A) over skip-if-cached (Option B) — architecturally sound, no staleness problem
- Deferred XPT read caching (Option C) — not needed yet, startup is ~1s without it
- ResizeObserver for chart width over CSS-only approaches — viewBox width must track container to maintain fixed row height
- Strip plot over hiding the chart — shows per-animal data not available in any other pane
- SVG height set explicitly (`style={{ height: chartHeight }}`) instead of `h-auto` — decouples height from aspect ratio
</decisions_made>

<blockers>
None. Strip plot is new feature work, not blocked.
</blockers>

<context>
User noticed >1min view load times — traced to init_validation() reading all XPT files at startup. Fixed by deferring to lazy load. User also noticed recovery chart SVGs collapsing at narrow panel widths (200-600px resizable context panel) — fixed with ResizeObserver-based dynamic viewBox. User asked about replacing SVG charts with ECharts — advised against it (resize is a container problem, not a charting library problem). User then asked about single-timepoint endpoints like basophils wasting space with a line chart — recommended strip plot, user agreed. BUG-11 logged for reference line label overlap at narrow widths (P3 cosmetic).
</context>

<next_action>
Start with: Read `.planning/handoff-strip-plot.md`, then confirm design decisions with user (threshold, pane title, mean markers), then implement backend endpoint first.
</next_action>
