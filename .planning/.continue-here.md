---
task: render-deployment
status: in_progress
last_updated: 2026-03-10T19:10:41.163Z
---

<current_state>
Deploying SENDEX to Render.com (free tier). Docker build now succeeds and app starts,
but user reported "analysis data not available" — last fix (bd5d553) addressed study_id
mismatch between XPT discovery path and generated/ directory. Awaiting user confirmation
that this fix resolved the issue on Render.
</current_state>

<completed_work>
- README updated: WIP notice with hosted demo link + GitHub issues link
- README screenshot: added docs/img/sendex-app.png, unignored docs/img/*.png
- Co-author removal: stripped Co-Authored-By from 13 commits via filter-branch, force pushed
- Dockerfile fix 1: added COPY shared/ ../shared/ for syndrome-definitions.json import
- Dockerfile fix 2: config.py now reads SEND_DATA_DIR from env var (was hardcoded)
- Dockerfile fix 3: flattened XPT copy path (send/PointCross/PC201708_XPT/ → /app/data/PointCross/) so study_id matches generated/PointCross/
- Endpoint context pane restructure (commit a00036e): rename Evidence→Statistical Evidence, Context→Effect Ranking, deduplicate bullets, three-state Patterns pane
</completed_work>

<remaining_work>
- Verify Render deployment loads analysis data after last fix (bd5d553)
- If still failing: add debug logging to lifespan startup to print discovered study IDs and check generated/ contents
- Consider adding backend/.venv-core/ and backend/_core_engine/ to .dockerignore (large, unneeded in Docker)
- Untracked frontend/src/components/analysis/NoaelDecisionView.tsx appeared from stash conflict — investigate whether it should be deleted
- Other unstaged work exists (backend correlations, context_panes, analyses router) — unrelated to deployment
</remaining_work>

<decisions_made>
- Used git filter-branch --msg-filter (not filter-repo) since filter-repo wasn't installed
- Chose to flatten XPT path in Dockerfile rather than rename generated/ or change discovery logic
- Made SEND_DATA_DIR configurable via env var to support both local dev and Docker
</decisions_made>

<blockers>
- GitHub contributor cache: Claude still shows as contributor, may take up to 24h to refresh
</blockers>

<context>
Session focused on getting the app presentable and deployed for external feedback.
The .dockerignore excludes *.md files — not an issue currently but worth noting.
The backend/.venv-core/ and backend/_core_engine/ untracked directories are large
and get copied into Docker via COPY backend/ ./ — should add to .dockerignore.
</context>

<next_action>
Check Render deploy logs after latest push. If "analysis data not available" persists,
add a debug print in main.py lifespan to list contents of /app/generated/PointCross/
and the discovered study IDs.
</next_action>
