---
task: render-deployment
status: fix_deployed
last_updated: 2026-03-10T19:59:32.220Z
---

<current_state>
Root cause found and fix pushed. Awaiting Render deploy (~3-5 min) and user verification.

The bug: the SPA catch-all route `/{full_path:path}` in main.py was intercepting unmatched
`/api/*` requests and returning index.html (200, text/html). The frontend's `fetchJson()`
tried to parse HTML as JSON, threw, and React Query surfaced it as "Analysis data not available."

Specific failing routes: `/api/studies/PointCross/validation` and
`/api/studies/PointCross/temporal/recovery-assessment` — both returned HTML instead of 404 JSON.
</current_state>

<completed_work>

- Diagnosed XPT path mismatch (commit bd5d553, prior session) — this was correct but insufficient
- Added diagnostic startup logging to main.py lifespan (commit a3fbc09)
- Added `/api/debug/health` endpoint — confirmed all data present (28 xpt, 19 json)
- Added build-time data verification + auto-generation in Dockerfile (commit 49854eb)
- Added runtime auto-generation at startup for missing studies (commit 49854eb)
- Excluded `.venv-core/`, `_core_engine/`, `.planning/` from .dockerignore (commit 49854eb)
- **Found root cause**: SPA catch-all serving HTML for unmatched `/api/*` paths
- **Fixed**: Added `if full_path.startswith("api/"):` guard to return 404 JSON (commit 108c374)
- Pushed fix to origin/master — Render auto-deploy should pick it up
</completed_work>

<remaining_work>

- Verify the fix works on Render after deploy completes (user to hard-refresh with Ctrl+Shift+R)
- If fix works: remove diagnostic logging from main.py (the `[DIAG]` prints and `/api/debug/health` endpoint)
- If fix doesn't fully work: check browser DevTools Network tab for any remaining HTML-instead-of-JSON responses
- Consider: some routes like `/api/studies/{id}/validation` returned HTML — check if those routes actually exist on the validation router or if the frontend expects them
</remaining_work>

<decisions_made>

- SPA catch-all must exclude `/api/` prefix — unmatched API routes should return 404 JSON, not index.html
- Added self-healing: Dockerfile runs generator at build time if generated/ missing; main.py runs generator at startup if missing — belt-and-suspenders for data availability
- Kept diagnostic logging for now — useful for monitoring Render deploys until stable
</decisions_made>

<blockers>
None — fix is pushed, awaiting deploy verification.
</blockers>

<context>
The misleading part of this bug: all 18 analysis view endpoints (`/api/studies/PointCross/analysis/*`)
returned 200 correctly. The failure was on OTHER router endpoints (validation, temporal, etc.) that
the frontend also calls. These unmatched routes fell through to the SPA catch-all and returned HTML.
The frontend error message was generic ("Analysis data not available") so it wasn't clear which
specific API call was failing.

The debug endpoint at `/api/debug/health` is still deployed — useful for future troubleshooting.
</context>

<next_action>
User verifies the fix on https://send-data-browser.onrender.com after Render deploys (3-5 min).
If working: clean up diagnostic code. If not: check browser Network tab for failing requests.
</next_action>
