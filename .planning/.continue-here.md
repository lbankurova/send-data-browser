---
phase: distribution-pane-vertical-strip-plot
task: mode-selector
status: blocked
last_updated: 2026-03-07T17:23:07.946Z
---

<current_state>
Strip plot vertical orientation committed (2 commits). Mode selector (Terminal/Peak/Recovery),
shared N hook, and peak-day algorithm implemented but NONE of the changes render in the
Findings view UI. Even the committed vertical chart doesn't appear — user still sees old
horizontal layout with old N values.
</current_state>

<completed_work>
- Commit 4ce5079: StripPlotChart.tsx — vertical orientation (groups X, values Y)
- Commit ebc6436: hover highlight works while dose selected
- DistributionPane.tsx: mode selector UI, peak/recovery modes, data transforms
- useRecoveryPooling.ts: NEW shared hook for recovery-pooling decision
- TimeCoursePane.tsx: refactored to use shared useRecoveryPooling hook
- .planning/handoff-distribution-pane.md: full technical handoff with algorithm details
</completed_work>

<remaining_work>
- BLOCKER: Find why changes don't render in Findings view (see below)
- Validate peak day algorithm (change-from-baseline) returns Day 15 not 43
- Validate N consistency between TimeCoursePane and DistributionPane
- Test Recovery mode with recovery cohort data
- Broader N filtering: death exclusion, TK animals (future)
</remaining_work>

<decisions_made>
- Vertical strip plot: groups on X with colored underlines, values on Y, F/M side by side
- Peak algorithm: change-from-baseline difference |(trtDelta - ctrlDelta)| per day
  - Absolute diff and % diff both fail for growing animals (monotonically increasing)
- useRecoveryPooling: shared hook replacing duplicate logic in TimeCoursePane/DistributionPane
- Label "Peak" not "Nadir" — user requested no jargon
</decisions_made>

<blockers>
UI NOT RENDERING: No changes visible in Findings view — not even committed vertical chart.
FindingsContextPanel.tsx imports DistributionPane (line 23, renders line 1282). Vite serves
new code (verified via curl). Build clean. Server restarted. Hard refresh done.

Hypothesis: The Findings view may render a different component or the DistributionPane
conditional path isn't being hit for the user's selection.
</blockers>

<context>
User is on Findings view > endpoint context panel > Distribution pane for BW endpoint.
Both FindingsContextPanel and DoseResponseContextPanel import DistributionPane.
Detailed technical notes in .planning/handoff-distribution-pane.md.
</context>

<next_action>
1. Add console.log or visible red div inside DistributionPane to confirm it mounts
2. If not visible: search ALL .tsx for alternate strip/distribution rendering in Findings view
3. If visible but no mode buttons: trace hasPeak, peakDay, showModeSelector values
</next_action>
