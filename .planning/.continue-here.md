---
phase: distribution-pane-fixes
task: peak-algorithm-and-cleanup
status: paused
last_updated: 2026-03-07
---

<current_state>
Distribution pane mode selector (Terminal/Peak/Recovery) is functional. UI renders,
mode switching works, vertical strip chart works, N values match TimeCoursePane for
Terminal mode. Peak mode shows Day 15 for PointCross but algorithm is wrong (works
by coincidence). Six items remain — see .planning/distribution-pane-fixes.md.
</current_state>

<completed_work>
- Commit b10fa25: StripPlotChart.tsx vertical orientation + hover highlight
- Commit 3217417: PanePillToggle extracted as reusable component, DistributionPane refactored
- DistributionPane.tsx: mode selector, three-layer N filtering (excludedSubjects +
  terminal_sacrifice_day + includeRecovery), peak day detection, recovery mode
- useRecoveryPooling.ts: NEW shared hook (used by DistributionPane + TimeCoursePane)
- TimeCoursePane.tsx: refactored to use useRecoveryPooling
- timecourse.ts: added terminal_sacrifice_day to TimecourseSubjectResponse
- UI rendering blocker from previous session: RESOLVED (was stale HMR cache)
</completed_work>

<remaining_work>
All items in .planning/distribution-pane-fixes.md — address in one pass:

1. CRITICAL: Fix peak algorithm — current min(absolute BW) is wrong.
   Correct: argmin[BWmean_high(t) - BWmean_control(t)]
   The day where high-dose mean is farthest below control mean.

2. CRITICAL: Peak strip plot should show delta from control, not raw BW.
   delta_i = BW_i(t*) - BWmean_control(t*)

3. Fix stale module comment at DistributionPane.tsx:6
4. Migrate DoseResponseView:1139-1140 to useRecoveryPooling
5. Add vitest test for findPeakEffectDay
6. Create docs/systems/distribution-pane.md
</remaining_work>

<decisions_made>
- Peak label: "Peak" not "Nadir" (no jargon)
- N filtering: three layers — excludedSubjects (user-controlled TR death exclusion),
  terminal_sacrifice_day matching (natural data availability), includeRecovery (pooling).
  All reuse existing infrastructure (useScheduledOnly, useRecoveryPooling).
- Peak algorithm must use delta from control, not absolute BW — absolute is confounded
  by age/growth. Formula: argmin[BWmean_high(t) - BWmean_control(t)]
- Peak strip plot dots: show delta_i = BW_i(t*) - BWmean_control(t*), not raw weight
</decisions_made>

<uncommitted_files>
- frontend/src/hooks/useRecoveryPooling.ts (NEW)
- frontend/src/components/analysis/panes/TimeCoursePane.tsx (MODIFIED — uses useRecoveryPooling)
- frontend/src/types/timecourse.ts (MODIFIED — added terminal_sacrifice_day)
- .planning/distribution-pane-fixes.md (NEW — implementation plan)
</uncommitted_files>

<next_action>
1. Read .planning/distribution-pane-fixes.md
2. Fix items 1-2 (peak algorithm + delta display) — critical bugs
3. Fix items 3-6 (comment, DoseResponseView, test, spec)
4. Commit all together after COMMIT-CHECKLIST
</next_action>
