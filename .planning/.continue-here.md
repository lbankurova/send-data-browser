---
phase: gsd-harness-setup
task: 4
total_tasks: 4
status: complete
last_updated: 2026-03-05
---

<current_state>
GSD harness setup complete. Doc lifecycle rule added, docs/incoming/ archived, gaps migrated to TODO.md.
</current_state>

<completed_work>

- Task 1: GSD harness setup — Done
  - `.planning/PROJECT.md` — brownfield context, validated requirements from existing code
  - `.planning/config.json` — YOLO mode, quality models, parallel execution
  - `.planning/STATE.md` — workstream tracking, backlog pointer to TODO.md
  - Committed: `9625ed7`

- Task 2: CLAUDE.md trim — Done
  - Removed rotting code state (architecture inventory, route/component counts, file counts, implementation status)
  - Added pointer to `.planning/codebase/` instead
  - 180→122 lines, committed: `d4cd317`
  - Condensed verbose explanations, merged related design rules
  - 122→109 lines (~1014 words), committed: `2a8d837`

- Task 3: Doc lifecycle strategy — In progress, agreed on approach but not yet written
</completed_work>

<remaining_work>

- Task 3: Write doc lifecycle process rule in CLAUDE.md (~3-4 lines). The agreed approach:
  - Specs are disposable — archive after implementation + review
  - Extract durable knowledge into `docs/knowledge/` or `docs/systems/`
  - Open gaps go to `docs/TODO.md`
  - Kill the TOPIC hub layer (don't create new ones, existing ones stay as frozen history)
  - System specs (`docs/systems/`) get updated when their subsystem ships changes
  - Rule replaces current TOPIC hub workflow (CLAUDE.md rule 7 needs updating)

- Task 4: (Optional) Audit existing docs against new lifecycle
  - Identify which specs in `docs/incoming/` can be archived now
  - Identify which TOPIC hub content should migrate to system specs
  - This is a larger effort — may warrant its own session
</remaining_work>

<decisions_made>

- Lightweight GSD harness over full roadmap — exploratory workflow needs flexibility, not rigid phases
- TODO.md stays as backlog — no redundant REQUIREMENTS.md or ROADMAP.md
- CLAUDE.md Design Decisions stay in CLAUDE.md (not skills) — Hard Rule #3 anchors them as tamper-resistant source of truth
- Doc lifecycle: specs → implement → review → archive spec → update system spec + knowledge (replaces spec → rot → TOPIC hub patch cycle)
- TOPIC hubs: stop creating new ones; existing 10 stay as frozen historical references
</decisions_made>

<blockers>
None
</blockers>

<context>
User is the domain expert and sole developer of a complex toxicology data explorer.
Workflow is exploratory — UI restructuring, engine fixes, and testing happen in parallel.
Priorities shift based on what surfaces during UI testing.
The doc structure has 4 overlapping layers (specs, TOPIC hubs, system specs, knowledge docs)
that need consolidation into 2 durable layers (systems/ + knowledge/) with archived specs for history.
Current CLAUDE.md rule 7 ("consult TOPIC hubs") will need rewording to reflect the new lifecycle.
</context>

<next_action>
Start with: Add doc lifecycle process rule to CLAUDE.md. Specifically:
1. Add a new Hard Process Rule (or amend rule 7) covering the spec→archive→extract lifecycle
2. Update rule 7 to clarify TOPIC hubs are frozen historical references, not actively maintained
3. Consider whether `docs/systems/` update-on-change should be a commit checklist item
</next_action>
