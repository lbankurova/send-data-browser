topic: subject-bw-group-lines
title: "Subject BW trajectory — group mean lines + 95% CI bands"
phase: complete
current_step: complete

checkpoints:
  blueprint.0:
    timestamp: "2026-04-05T15:00:00"
    summary: >
      Implementation context loaded. No failed approaches relevant.
      No bug-patterns.md. om-bw-scatter shipped (SVG chart pattern reuse).
      SubjectProfilePanel not domain-critical. getDoseGroupColor is canonical.
      ci95Half() in TimeCoursePane.tsx is the CI calculation to reuse.
    constraints:
      - SubjectProfilePanel design is frozen — requires user approval for visual changes (CLAUDE.md)
      - User has explicitly requested this change — approval implicit
      - Must match TimeCourseLineChart rendering exactly (CI bands + group lines)
      - Y-axis mode is always "absolute" (raw BW values)
      - Filter group data to subject's sex only
  build.complete:
    timestamp: "2026-04-06T00:00:00"
    commit: 5d5d30fc
    summary: >
      BWSparkline enhanced with group mean lines + 95% CI bands (control + subject's
      dose group only per user direction). Interactive filter legend. Terminal sacrifice
      clipping. Dashed recovery reference line. ci95Half extracted to shared stats-utils.ts.
    user_corrections:
      - "Only control + subject's dose group, not all groups"
      - "Group lines must not extend past terminal sacrifice day"
      - "Add dashed vertical reference line at terminal sacrifice for recovery subjects"
      - "Legend shows groups only, not subject entry"
      - "Recovery group extension not needed — different cohorts, would be misleading"
