# Continue Here

<!-- last_updated: 2026-03-09T14:55:28.192Z -->

## Completed This Session

1. **Recovery assessment audit** — Full-stack audit of recovery subsystem (scientific + statistical + verdict logic). Identified 10 issues across backend and frontend, ranked by severity. User provided expert domain feedback correcting priorities and identifying implementation traps.

2. **Fix 1: Guard chain ordering** (m-1) — Swapped not_observed before low_power in `computeVerdict()`. Clean negatives were misclassified as "low_power" instead of "not_observed" because `0 * N < 2` is always true when main.incidence=0. Tests updated.

3. **Fix 2: CL incidence worsening verdict** (M-2) — Added "worsening" verdict when recovery incidence exceeds main incidence. Previously collapsed into "persistent", hiding delayed-onset toxicity signals. Backend, frontend types, UI labels/colors, system doc updated.

4. **Fix 3: CL time-period filter** (m-4) — Recovery-arm CL records now filtered to post-dosing only via `compute_last_dosing_day()`. Treatment-phase observations from recovery animals were inflating recovery incidence counts.

5. **Commit**: `9073f09 fix: recovery assessment — 3 verdict logic bugs` — 1338 tests pass, build clean.

## Decisions Made

- **Guard chain**: not_observed must precede low_power. Anomaly guard already handles recovery>0/main=0 case.
- **CL worsening**: red-700 color (same tier as new_in_recovery). Added between improving and persistent in conditional chain.
- **CL time filter**: Preserves null-day records (defensive). Falls back to unfiltered when last_dosing_day unavailable.
- **M-3 (examination heuristic)**: Naive fix is wrong — needs MA domain join to know if tissue was collected. Higher effort, specced for later.
- **M-1 (continuous confidence)**: CI surfacing is wrong UX for tox reviewers. Use verdict modifier string instead.
- **Anomaly priority**: Keep anomaly highest — least explainable finding demands most human review.

## Still Pending

### Recovery audit fixes (specced in `docs/incoming/recovery-assessment-audit.md`)
- **Fix 4 (M-3)**: Examination heuristic — MA domain join. HIGH EFFORT. Backend must read MA domain per subject.
- **Fix 5 (M-1)**: Continuous verdict confidence qualifier. MEDIUM EFFORT. Add `confidence: "low"` when n < 5.
- **Investigation**: Sex stratification — verify verdict engine handles sex-restricted recovery arms.
- **Investigation**: Peak effect denominator — confirm terminal-only reference in verdict computation.

### Prior work (from previous session)
- **Phase 3: Migrate pane-form overrides** — ToxFindingForm, OrganContextPanel, PathologyReviewForm, ValidationIssueForm. OverridePill in CollapsiblePane `headerRight`.
- **Phase 4: Consistency pass** — All overrides show user/timestamp, dependency chains use thin red border.
- **BUG-16**: Pattern -> onset dose dependency invalidation logic.
- **BUG-07**: Recovery dumbbell chart broken adaptive rendering on panel resize.
- Full backlog: `docs/TODO.md`.

## Resume

Two paths:
1. **Recovery fixes**: Pick up Fix 4 or 5 from `docs/incoming/recovery-assessment-audit.md`. Sex-stratification investigation is a quick read first.
2. **Override migration**: Phase 3 of `docs/incoming/unified-override-pattern.md`. Read `ToxFindingForm.tsx`, `OrganContextPanel.tsx`, etc.
