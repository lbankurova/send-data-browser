# Continue Here

<!-- last_updated: 2026-03-09T13:57:46.769Z -->

## Completed This Session

1. **Phase 1 — Extract OverridePill** (`faca062`). Created `components/ui/OverridePill.tsx` with `OverridePill` + `OverrideCell` exports. Extracted from MortalityDataSettings OverrideDot. Props: `isOverridden`, `note`, `user`, `timestamp`, `onSaveNote`, `placeholder`, `autoOpen`, `popoverSide/Align`. OverrideCell adds `needsAttention` (thin red border) + `attentionTooltip`. Tooltip via native `title` (GlobalTooltip intercepts).

2. **Phase 2 — Migrate table-cell overrides** (`18f74df`). Pattern dropdown: replaced `*` asterisk + inline Popover with OverridePill. Onset dose dropdown: same, plus fixed `border-b-2` to `border-b` (canonical thin red). Mortality: wired override comments to annotation API (`mortality-overrides`) with optimistic cache updates — comments now persist across sessions. Added `mortality-overrides` to backend `VALID_SCHEMA_TYPES`. Net -64 lines.

## Decisions Made

- **OverridePill uses native `title` for tooltip**, not Radix Tooltip (avoids conflict with Popover on same trigger; GlobalTooltip intercepts `title` anyway).
- **OverrideCell is exported from same file** as OverridePill (thin wrapper, not worth a separate file).
- **Mortality persistence uses optimistic cache updates** — `queryClient.setQueryData` before mutation fires, so UI feels instant.
- **No roadmap/milestone for this work.** Exploratory refactoring — spec-driven, iterative commits.

## Still Pending

- **Phase 3: Migrate pane-form overrides** — Tox assessment, normalization, pathology review, validation forms. OverridePill in CollapsiblePane `headerRight`, standardize footer.
- **Phase 4: Consistency pass** — All overrides show user/timestamp in tooltip, all dependency chains use thin red border, remove all legacy indicators (`*`, `(overridden)`, `(user override)`).
- **BUG-16**: Pattern -> onset dose dependency invalidation logic (false positives, no_change edge case).
- **BUG-07**: Recovery dumbbell chart broken adaptive rendering on panel resize.
- Full backlog: `docs/TODO.md` (73 open items).

## Resume

Next: Phase 3 of `docs/incoming/unified-override-pattern.md`. Read `ToxFindingForm.tsx`, `OrganContextPanel.tsx` (normalization), `PathologyReviewForm.tsx`, `ValidationIssueForm.tsx`, `ValidationRecordForm.tsx`. For each: replace text indicators (`(overridden)`, `(user override)`) with OverridePill in CollapsiblePane `headerRight` slot.
