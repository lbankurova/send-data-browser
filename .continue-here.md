# Continue Here

<!-- last_updated: 2026-03-13T20:56:17Z -->

## Current Work: View Merge — Phases A+B done, gap fixes done, ready for Phase C

Branch: `merge-findings-dr`

### Completed This Session (not yet committed)

**Post-implementation review for Phases A+B** identified 6 gaps (GAP-74–79 in TODO.md). All 6 gaps fixed:

1. **GAP-79 (P3)**: Extracted duplicate `shortDoseLabel()` to `frontend/src/lib/dose-label-utils.ts`. Both `TimeCoursePane.tsx` and `TimeCourseBarChart.tsx` now import from shared utility.

2. **GAP-74 (P1)**: Added `ToxFindingForm` to `FindingsContextPanel` after CausalityWorksheet. Wired `deriveToxSuggestion` from `signalSummary` signal row matching the selected finding's endpoint_label + sex.

3. **GAP-76 (P2)**: Fixed InsightsList filter — now uses dual filter (organ_system OR domain-prefix endpoint-scope rules), matching the D-R panel pattern. New `endpointRules` memo in FindingsContextPanel.

4. **GAP-77 (P3)**: Reordered FindingsContextPanel panes to match view-merge-spec §5 target:
   Header → Verdict → CausalityWorksheet → ToxFindingForm → EvidencePane → InsightsList → DoseDetail → TimeCourse → Distribution → Recovery → Patterns → Correlations → Effect Ranking → Related views.

5. **GAP-75 (P2)**: Added header incremental info to `ContextPanelHeader` via children prop: pattern badge (neutral gray, `getPatternLabel`), assessment status badge (from `toxAnnotations`), NOAEL indicator.

6. **GAP-78 (P3)**: Added Timepoint toggle (Terminal/Peak/Recovery) to `TimeCoursePane`. Peak day = day of max |effect| for highest dose. Recovery day = last day after terminal. Uses `PanePillToggle`, only shown when options available.

### Build & Test Status

- `npm run build`: PASSES
- `npm test`: 1459/1459 PASSES
- No uncommitted changes staged

### Files Modified (not committed)

**New files:**
- `frontend/src/lib/dose-label-utils.ts` — shared `shortDoseLabel()` utility
- `frontend/src/components/analysis/panes/TimeCourseBarChart.tsx` — CL bar chart (Phase B)

**Modified files:**
- `frontend/src/components/analysis/panes/FindingsContextPanel.tsx` — GAP-74/75/76/77 (ToxFindingForm, header info, InsightsList filter, pane reorder)
- `frontend/src/components/analysis/panes/TimeCoursePane.tsx` — Phase B + GAP-78/79 (Y-axis modes, subject traces, timepoint toggle, shared utility import)
- `frontend/src/components/analysis/panes/TimeCourseLineChart.tsx` — Phase B (ChartPoint generalization, SubjectTrace, yTickFormatter)
- `frontend/src/hooks/useTimeCourseData.ts` — Phase B (RawGroupPoint, controlByDay)
- `frontend/src/components/analysis/FindingsTable.tsx` — prior session (table modes, not yet visible)
- `docs/TODO.md` — GAP-74–79 logged

### What's Next

1. **Commit** — Phase B + gap fixes are ready to commit (build + tests pass)
2. **Phase C** — next phase per `docs/incoming/view-merge-spec.md` §16 implementation order
3. **FindingsTable visibility issue** — coded changes from prior session are not showing in browser (see debugging notes below)

### Prior Session: FindingsTable Not Visible

Three items coded in FindingsTable.tsx (Type column, dose header tooltips, All/Worst mode toggle) — build passes, Vite serves updated code, but user reported changes not visible in browser. Possible causes:
- Vite HMR stuck — try killing all node processes and restarting fresh
- Browser cache — try incognito
- CSS/layout issue — header bar might be in DOM but invisible
- See prior `.continue-here.md` for full debugging notes

## Also Pending

- Wire HELP.md into the app
- Move deck folder into pcc repo
- Cross-check HELP.md against data-lineage.md
- Archive `docs/incoming/sla-fix-spec.md`
- Full backlog: `docs/TODO.md`
