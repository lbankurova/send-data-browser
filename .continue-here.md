# Continue Here

<!-- last_updated: 2026-03-11T21:58:57Z -->

## Completed This Session

1. **Post-implementation review of `docs/incoming/sla-fix-spec.md`** — full 6-step process per `docs/checklists/POST-IMPLEMENTATION-REVIEW.md`. Identified 4 unimplemented + 9 partial SLA gaps, logged as GAP-72 in TODO.md.

2. **Decision comment on `effectSizeLabel()`** — documents why `"|d|"` not `"|g|"` in both `domain-types.ts` and `send_knowledge.py`. Codebase uses `cohens_d` field names; difference <2% at typical group sizes.

3. **INCIDENCE_DOMAINS semantic cleanup** — reduced from 5→4 members (removed MI, which is severity_grade not incidence). Migrated 14 callsites to `!CONTINUOUS_DOMAINS.has()`. Eliminates semantic trap.

4. **Maintenance rule comment** added above `CONTINUOUS_DOMAINS` in both frontend (`domain-types.ts`) and backend (`send_knowledge.py`). Documents asymmetric failure modes.

5. **Committed as `b2b7c58`.** All tests pass (1430 frontend, 25 backend).

## Decisions Made

- **`"|d|"` over `"|g|"`**: Codebase consistency — backend uses `cohens_d` field names. Spec hedged with "or |d| depending on stat method setting."
- **INCIDENCE_DOMAINS = {MA, CL, TF, DS}**: MI excluded — severity_grade, not incidence. Semantic honesty over convenience.
- **Callsite pattern**: `!CONTINUOUS_DOMAINS.has()` (positive-form) reads as "this domain lacks a magnitude scalar."
- **Asymmetric failure principle**: Missing from CONTINUOUS_DOMAINS = visible (no effect size displayed). Incorrectly including = silently wrong scientific conclusions. "When in doubt, omit."

## Still Pending

### Spec archival
- `docs/incoming/sla-fix-spec.md` → archive to `docs/incoming/archive/` per CLAUDE.md rule 7
- Extract durable knowledge to `docs/knowledge/` or `docs/systems/`

### GAP-72 (SLA remaining work, in TODO.md)
- **Unimplemented**: SLA-09 (incidence quality checks), SLA-15 (CL recovery MIN_RECOVERY_N), SLA-16 (corroboration direction coherence), SLA-18 (recovery vocabulary harmonization)
- **Partial**: SLA-01 labels, SLA-02 frontend signal boost, SLA-06 table sort, SLA-06/12 local set duplication, SLA-07 fractional INHAND grade, SLA-13 column header, Phase 0 ~20+ display callsite migration

### Prior pending items
- Recovery audit investigations: sex stratification, peak effect denominator
- BUG-16: Pattern onset dose dependency invalidation
- BUG-07: Recovery dumbbell chart adaptive rendering
- Full backlog: `docs/TODO.md`

## Resume

1. **Archive sla-fix-spec.md** — move to `docs/incoming/archive/`, extract durable bits
2. **GAP-72 items** — pick next priority from TODO.md
3. **Backlog** — `docs/TODO.md` for full list
