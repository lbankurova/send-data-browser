# Continue Here

> Paused: 2026-04-03
> Branch: master
> Task: Swimmer plot design for cohort view (Phase 5)

## Where I stopped

Design phase — user rejected my proposed design and will return with their own. No code was written. One HTML mockup was created but does not reflect the final design.

## What's done

- **Full data exploration** — mapped all per-subject temporal data available in PointCross:
  - `subject_onset_days.json`: 103 subjects, CL/LB/MI/MA domains, mostly terminal (Day 92/106)
  - BW time-series: 14+ points/subject via `useSubjectComparison` hook
  - FW food consumption: periodic measurements in `fw.csv`
  - Recovery verdicts: 30 subjects, per-finding verdicts
  - NOAEL overlay: per-subject attribution with driving findings
  - LOO influential: per-finding from unified_findings pairwise data
  - OM alternatives: ratio_to_bw available via `alternatives` key
- **Critical data finding** — typical high-dose subject has ~108 findings but only 1-3 with temporal variation (CL). The other 100+ are terminal MI/MA all at Day 92/106. This rules out a naive "one horizontal lane per finding" swimmer.
- **Spec inventory** — catalogued all features the spec planned: BW+food efficiency toggle, OM organ-to-BW ratios, RCV overlay (graceful degradation), recovery phase extension with verdict coloring, LOO influential markers, NOAEL-driving markers, dosing phase demarcation
- **Outlier engine status** — confirmed LOO influential animal engine is fully built and production-ready. RCV (Reference Change Values) research complete but backend not implemented.
- **HTML mockup** — `frontend/e2e/mockups/swimmer-plot.html` with real PointCross data (PC201708-2013). This mockup was rejected for:
  1. Not matching the spec's layout (signal table always visible, chart section above/below)
  2. Design system violations (colored domain tags, severity colors at rest, >10% saturated pixels)

## What's next

1. **User will provide their own design** — wait for user to return with a design direction
2. **When design is provided, implement** — the data sources and hooks are well-understood (see "Key data sources" below)
3. **Delete mockup** — `frontend/e2e/mockups/swimmer-plot.html` is disposable

## Decisions made this session

- **Two-section approach was proposed but NOT approved** — I proposed splitting temporal findings (SVG timeline) from terminal findings (compact organ summary). User rejected this and the overall design.
- **Terminal findings are the dominant data** — ~100 of ~108 findings are terminal MI/MA. The swimmer design MUST account for this — naive horizontal lanes won't work.
- **Outlier engine IS built** — LOO influential animal detection is production-ready. Don't assume it's missing.
- **RCV not yet available** — research done, backend not implemented. Degrade gracefully.
- **OM ratio_to_bw available** — via `alternatives` key in OM findings, user-selectable

## Blockers / open questions

- **Design direction** — user will provide their own. Key tension: the spec envisions horizontal temporal lanes, but real data is 95% terminal with no temporal variation. How to reconcile?
- **What belongs in the swimmer vs context panel vs the signal table?** — not resolved
- **Height allocation** — spec says 65/35 but user pushed back that signal table must remain primary

## Key data sources for implementation

| Data | Hook/File | Per-subject? | Temporal? |
|------|-----------|-------------|-----------|
| Finding onset days | `useOnsetDays()` -> `subject_onset_days.json` | Yes | Single point per finding |
| BW trajectory | `useSubjectComparison()` -> temporal API | Yes | 14+ timepoints |
| FW consumption | `fw.csv` via FW findings | Yes | Periodic |
| Recovery verdicts | `useRecoveryVerdicts()` -> `recovery_verdicts.json` | Yes | Verdict only, no day-by-day |
| NOAEL attribution | API -> `subject_noael_overlay.json` | Yes | N/A |
| LOO influential | `unified_findings.pairwise[].loo_influential_subject` | Per-finding | N/A |
| MI/MA severity | `histopathMap` in CohortContext | Yes | Terminal only |
| OM alternatives | `unified_findings.alternatives.ratio_to_bw` | Group-level | Terminal only |
| Study timeline | `study_metadata_enriched.last_dosing_day` = 91 | Study-level | N/A |

## Key files (not touched, but read this session)

- `frontend/src/components/analysis/CohortView.tsx` — main view layout
- `frontend/src/components/analysis/cohort/CohortCharts.tsx` — current chart strip (180px, BW + organ metrics)
- `frontend/src/components/analysis/cohort/SubjectSignalTable.tsx` — primary table
- `frontend/src/contexts/CohortContext.tsx` — state management
- `frontend/src/components/analysis/panes/RecoveryDumbbellChart.tsx` — SVG reference pattern
- `frontend/src/lib/severity-colors.ts` — color utilities
- `frontend/src/lib/recovery-verdict.ts` — verdict types and classification
- `docs/_internal/incoming/cohort-view-redesign-synthesis.md` — Phase 5 spec (lines 131-165)
- `docs/_internal/designs/cohort-center-panel-states.md` — current center panel states

## Uncommitted changes

30 files modified — mix of prior sessions (LOO viz, HCD, Williams, strip plot, radar scoring). No swimmer code written.
