---
feature: portfolio-auto-registration-and-view-toggle
status: in_progress
last_updated: 2026-03-28T20:35:55Z
---

## Current Work: Portfolio Auto-Registration + Studies/Portfolio View Toggle

### What's done

**Backend (fully done, tested):**
- `models/study_metadata.py` — Made StudyMetadata and Project fields Optional with defaults for auto-derived entries
- `services/study_metadata_service.py` — Added `register_discovered_studies()` that parses TS domain, extracts TRT as test_article, auto-creates Project entries per unique compound
- `main.py` — Calls `register_discovered_studies()` at startup after study discovery
- `routers/import_study.py` — Calls it on import too; also fixed zip cleanup bug (empty dir left behind); multi-file .xpt import support added
- `tests/test_find_xpt_callers.py` — AST guard ensuring all `_find_xpt_files()` callers unpack tuple
- Verified: 4 TOXSCI studies auto-register as compound 5492 (Compound A) and 6576 (Compound B)

**Frontend (mostly done, needs verification):**
- Nullable interface fields across `useStudyPortfolio.ts`, `useProjects.ts`
- Null guards in `StudyPortfolioView.tsx`, `ProgramNoaelsPane.tsx`, `RelatedStudiesPane.tsx`, `PackageCompletenessPane.tsx`, `AppLandingPage.tsx`
- `AppLandingPage.tsx`: Studies/Portfolio toggle; Portfolio mode shows flat `ProgramList` (programs, not studies)
- `SelectionContext.tsx`: Added `selectedProjectId` / `selectProject`
- `ContextPanel.tsx`: Portfolio context panel gated on `selectedProjectId` — only shows in portfolio mode, NOT for study clicks in studies mode
- `RelatedStudiesPane.tsx`: Studies are now clickable `<Link>` elements

### What's remaining

1. **Verify context panel routing** — The critical fix: studies mode click must show `StudyInspector` (original), portfolio mode program click must show `StudyPortfolioContextPanel`. Gate is `selectedProjectId` in `ContextPanel.tsx` line ~395.
2. **Run build + tests** — Last build was clean. Tests not run since last ContextPanel changes.
3. **Manual E2E test** of both modes.
4. **Commit** — All changes uncommitted.

### Key decisions
- Portfolio view = flat list of PROGRAMS (not grouped studies) — user rejected grouped-studies approach twice
- `selectedProjectId` gates the portfolio context panel — prevents it from hijacking study clicks in studies mode (user caught this bug twice)
- Auto-derived entries flagged with `auto_derived: true`
- ISO 8601 duration parsing for DOSDUR (P4W→4, P28D→4, P1M→4)

### Start here
1. `cd C:/pg/pcc/frontend && npm run build && npm test`
2. Start backend + frontend, test both modes
3. Commit if correct
