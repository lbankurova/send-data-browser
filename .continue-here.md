# Continue Here

<!-- last_updated: 2026-03-13T16:15:00Z -->

## Completed This Session

1. **HELP.md draft** — committed as `68d902e`
   - Datagrok-style technical help page at `docs/HELP.md`
   - Sourced from README, ARCHITECTURE, system docs, deck content
   - NOT yet verified against `SENDEX-data-lineage.md` (the code-verified ground truth) — must cross-check before finalizing
   - Covers: pipeline, views (skeleton), rules engine, syndromes, ECETOC assessment, annotations, settings, validation, SEND support, stack

2. **Staleness fixes** — same commit
   - `README.md`: syndromes 24 -> 33
   - `ARCHITECTURE.md`: cross-domain-syndromes.ts 10 -> 33 (XS + XC series)
   - `docs/systems/validation-engine.md`: 7 custom rules -> 14 (added FDA-001–FDA-007)

3. **Deck frozen** — PowerPoint kept for future use, not actively updated

## Branch State

- Branch: `merge-findings-dr`
- Commits ahead of master: 4 (b00df5e, 1c1f5ab, 32b97a2, 68d902e)

---

## Next: Three workstreams

### 1. Wire HELP.md into the app

Add a "Help" nav item in the browsing tree, after domain views, separated by a divider. Renders `docs/HELP.md` as a scrollable single-page HTML view.

Implementation:
- Add `react-markdown` (+ `remark-gfm` for tables) to frontend dependencies
- Create `HelpView.tsx` — fetches/imports the markdown, renders as scrollable page
- Add route: `/studies/:id/help` (or global `/help`)
- Add nav item in `BrowsingTree.tsx` after domain links, with a divider above it
- Style to match Datagrok help pages: clean typography, table styling, code blocks
- Views section is a skeleton — expand after the findings+DR view merge lands

### 2. Move deck folder into pcc repo

Move `C:\pg\pcc-sup\deck\` contents into `docs/deck/` in the pcc repo:

```
docs/deck/
  SENDEX-deck-content.md    — slide content (frozen)
  generate_deck.py          — PPTX generator (frozen)
  SENDEX-Presentation.pptx  — v1 with user's manual edits
  SENDEX-Presentation-v2.pptx — v2 generated (46 slides)
```

Move `SENDEX-data-lineage.md` to `docs/knowledge/data-lineage.md` — it's a code-verified reference doc, belongs with other knowledge docs.

After moving:
- Delete `C:\pg\pcc-sup\deck\` (or archive)
- Update `.continue-here.md` references
- Add `docs/deck/` to `.gitignore` for the .pptx files (binary, don't track)
  - OR: track them if user wants version history

### 3. Cross-check HELP.md against data-lineage.md

The lineage doc was specifically cross-checked against code (26 discrepancies fixed, 1 code bug fixed). HELP.md was drafted without consulting it. Must:
- Read `SENDEX-data-lineage.md` end to end
- Compare every factual claim in HELP.md (file names, row counts, pipeline steps, module names, output artifacts)
- Fix any discrepancies — lineage doc is the ground truth
- Also update any other stale docs found during review

### Also pending (from prior sessions)

- **Phase B of findings+DR merge** — Time-Course Unification (4 sub-tasks, see spec section 6)
- Archive `docs/incoming/sla-fix-spec.md`
- GAP-72 items in TODO.md
- Full backlog: `docs/TODO.md`

## Decisions Made

- Help page is a scrollable single-page markdown doc rendered in-app
- Named "Help" in nav
- Deck materials move to `docs/deck/` (frozen, update only when needed)
- Data lineage doc moves to `docs/knowledge/data-lineage.md`
- Sales-y content stripped — technical Datagrok style only
- Views section kept thin until merge completes
