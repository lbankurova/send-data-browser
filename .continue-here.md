# Continue Here

<!-- last_updated: 2026-03-08T18:50:01.917Z -->

## Completed This Session

1. **keepPreviousData fix** — Removed `placeholderData: keepPreviousData` from `useFindingContext.ts`. Root cause: React Query showed stale data from previous finding when switching endpoints, causing duplicated group labels and wrong table layouts. Added `no-stale-placeholder-data.test.ts` lint guard.

2. **CH=6 → CH=7** — Fixed `DoseDetailPane.tsx` colWidths character width constant to match DoseResponseView, preventing truncated numeric columns.

3. **CollapsiblePane border fix** — Removed `last:border-b-0` from `CollapsiblePane.tsx`. Wrapper divs with refs broke CSS sibling selector, causing inconsistent section separators between endpoints.

4. **Anomaly discrimination Step 3b** — Added same-finding dose extrapolation to `anomaly-discrimination.ts`. When a finding is treatment-related at higher doses in main arm, recovery-only occurrence at lower doses → `delayed_onset_possible` instead of `possible_spontaneous`. Updated `methods.md` CLASS-20. Added 2 tests.

## Commits

- `89874aa` — Mixed commit (hook grabbed all unstaged): keepPreviousData fix + CH fix + lint test + previous session's pattern override work
- `a71732d` — Empty duplicate (hook artifact)
- `2316952` — Anomaly discrimination Step 3b + CollapsiblePane border fix + methods.md

## Still Pending

- Pattern classification regression in `classification.py` — `_pooled_sd()` excludes control
- BUG-07: Recovery dumbbell chart broken adaptive rendering on panel resize
- Check TODO.md for full backlog

## Resume

No uncommitted work. Check TODO.md for next priorities.
