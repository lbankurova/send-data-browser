# Continue Here

<!-- last_updated: 2026-03-15T19:30:00Z -->

## Current Work: View Merge — Findings Table Improvements (Phase C items)

Branch: `merge-findings-dr`

### Completed This Session

1. **BUG-09 fixed**: Removed `unitLabel` from all dose column headers in FindingsTable (was showing "mg/kg" on control column). Unit info remains in tooltip only.

2. **Type column tooltip**: Added descriptive tooltip to the TYPE column header ("Data type: continuous (group mean) or incidence (affected/N)").

3. **Pivoted view**: Added Standard/Pivoted layout toggle to FindingsTable header bar. Pivoted mode transposes dose groups from columns to rows — one row per finding per dose group. Columns: Domain, Finding, Sex, Day, Dose (color-coded DoseLabel), N, Value, SD/%, P-value, Effect, Trend p, Pattern, Severity. Implements spec Q2 decision from `docs/incoming/view-merge-spec.md`.

4. **Open in tab**: Added ExternalLink icon to table header bar (visible in Chart tab only). Clicking it switches FindingsView to full-height Table tab. FindingsView now has ViewTabBar with Chart/Table tabs. Table tab has closable X on hover.

5. **ViewTabBar enhanced**: Added `closable` and `onClose` props to shared ViewTabBar component. Closable tabs show X on hover via group-hover opacity transition.

### Prior Commits This Session

1. **f944499** — `feat: Phase B — unified TimeCourse + gap fixes GAP-74–79`
2. **77ce6f6** — `fix: pane reorder (data→analysis→determination) + per-sex causality` (includes FindingsTable pivoted view, layout toggle, open-in-tab, Type tooltip, unitLabel removal)

### Build & Test Status

- `tsc --noEmit`: PASSES
- Tests: not re-run this session (no logic changes to tested code)

### Files Modified (not committed)

- `frontend/src/components/analysis/findings/FindingsView.tsx` — ViewTabBar integration, Chart/Table tabs, onOpenInTab wiring, close-tab handler
- `frontend/src/components/ui/ViewTabBar.tsx` — closable tab support (X on hover)

### What's Next

1. **Commit** remaining files (FindingsView.tsx, ViewTabBar.tsx) — build passes
2. **Test the pivoted view** against D-R metrics table data — verify no data loss per spec Q2
3. **Phase C** of view merge — see `docs/incoming/view-merge-spec.md` §16 implementation order
4. **Spec items not yet implemented:**
   - Group-by toggle (Findings Navigation — flat list with group-by)
   - Day-by-dose detail table removal
   - Pairwise table removal from dose detail pane

### Decisions Made

- "Open in tab" = ViewTabBar tab (Chart/Table), not a separate window or browser tab
- Pivoted view uses same All/Worst filter as standard view
- Close button on tab uses group-hover opacity pattern (invisible at rest, visible on hover)
- Control rows in pivoted view show dash for P-value and Effect (no comparison to self)

## Also Pending

- Wire HELP.md into the app
- Move deck folder into pcc repo
- Cross-check HELP.md against data-lineage.md
- Archive `docs/incoming/sla-fix-spec.md`
- Full backlog: `docs/TODO.md`
